[project]
name = "bid-bot"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "Flask==3.1.2",
  "requests==2.32.3",
  "psycopg2-binary==2.9.10",
  "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "api.run:app"
